<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="NemoclawChat_Windows.Pages.ArchivePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource AppBackgroundBrush}" Padding="44,36" RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="8">
            <TextBlock
                FontSize="30"
                FontWeight="SemiBold"
                Foreground="White"
                Text="Archivio" />
            <TextBlock
                Foreground="{StaticResource MutedTextBrush}"
                Text="Ricerca locale per chat, progetti e task recenti salvati davvero sul dispositivo." />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox
                x:Name="SearchBox"
                Header="Cerca"
                PlaceholderText="Cerca chat, progetto, server, task..."
                TextChanged="SearchBox_TextChanged" />
            <ComboBox
                x:Name="FilterBox"
                Grid.Column="1"
                Header="Filtro"
                SelectionChanged="FilterBox_SelectionChanged"
                SelectedIndex="0">
                <ComboBoxItem Content="Tutto" />
                <ComboBoxItem Content="Chat" />
                <ComboBoxItem Content="Progetto" />
                <ComboBoxItem Content="Task" />
                <ComboBoxItem Content="Server" />
            </ComboBox>
            <Button
                Grid.Column="2"
                Margin="0,28,0,0"
                Click="NewChat_Click"
                Content="Nuova chat" />
        </Grid>

        <Grid Grid.Row="2" ColumnSpacing="18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ResultsPanel" Spacing="12" />
            </ScrollViewer>

            <Border
                Grid.Column="1"
                Padding="20"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="#FF3A3A3A"
                BorderThickness="1"
                CornerRadius="18">
                <StackPanel Spacing="14">
                    <TextBlock Foreground="White" FontWeight="SemiBold" Text="Dettaglio" />
                    <TextBlock
                        x:Name="DetailTitleText"
                        FontSize="20"
                        FontWeight="SemiBold"
                        Foreground="White"
                        Text="Seleziona elemento" />
                    <TextBlock
                        x:Name="DetailBodyText"
                        Foreground="{StaticResource MutedTextBrush}"
                        Text="Qui vedi dettagli e prompt dell'elemento selezionato."
                        TextWrapping="Wrap" />
                    <Button
                        x:Name="OpenButton"
                        Click="OpenSelected_Click"
                        Content="Apri"
                        IsEnabled="False" />
                    <Button
                        x:Name="PinButton"
                        Click="PinSelected_Click"
                        Content="Segna progetto"
                        IsEnabled="False" />
                    <Button
                        x:Name="DeleteButton"
                        Background="#FF5A1F2B"
                        BorderBrush="#FF7A2B3A"
                        Click="DeleteSelected_Click"
                        Content="Elimina"
                        Foreground="White"
                        IsEnabled="False" />
                    <TextBlock
                        x:Name="StatusText"
                        Foreground="{StaticResource MutedTextBrush}"
                        Text="Pronto."
                        TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
