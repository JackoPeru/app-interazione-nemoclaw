<?xml version="1.0" encoding="utf-8"?>
<!--
    Hermes Hub e' un client LAN: si collega tipicamente a hermes.local o ad un IP privato
    senza certificato TLS valido. Per non rompere setup user-controlled (IP arbitrario
    nel range RFC1918) lasciamo cleartext permesso globalmente; pinning/HTTPS deve essere
    scelta dell'utente quando installa un gateway con TLS reale.
    Per release Store/firmate sarebbe meglio whitelistare solo hermes.local e disabilitare
    il resto: vedi audit-0.6.20.md.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
